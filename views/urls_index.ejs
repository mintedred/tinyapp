<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
    <ul>
      <% for (let key in urls) { %>
        <li><%= key %> : <%= urls[key] %> </li>
      <% }; %>
    </ul>
</body>
</html>